#ifndef _SORT_H_
#define _SORT_H_

/* HEADER FILES */
#include <stddef.h>
#include <stdlib.h>

/* PROTOTYPES */
void radix_sort(int *array, size_t size);
void print_array(const int *array, size_t size);
#endif /* SORT_H */
